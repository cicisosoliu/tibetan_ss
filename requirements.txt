torch>=2.1
torchaudio>=2.1
lightning>=2.2
torchmetrics>=1.3
hydra-core>=1.3
omegaconf>=2.3
numpy>=1.24
scipy>=1.11
soundfile>=0.12
librosa>=0.10
pandas>=2.0
tqdm>=4.66
pesq>=0.0.4
pystoi>=0.4.1
tensorboard>=2.15
einops>=0.7
matplotlib>=3.8

# Optional (model specific); install separately if needed:
#   pip install mamba-ssm causal-conv1d   # dual-path mamba
#   pip install speechbrain               # only if you want to reuse SB's IO layer
